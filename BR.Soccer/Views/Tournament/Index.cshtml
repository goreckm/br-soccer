@model IEnumerable<BR.Soccer.Models.Tournament>

@{
    ViewBag.Title = "Index";
}

<h2>Tournaments</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Name, "Details", new { id=item.TournamentId })
        </td>
    </tr>
}

</table>
